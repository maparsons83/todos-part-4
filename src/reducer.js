import {
    MARK_COMPLETE, 
    ADD_TODO,
    DELETE_TODO,
    CLEAR_COMPLETED
} from './actions.js'

export const todosReducer = (state, action) => {
    switch (action.type) {
        case MARK_COMPLETE:
        
        case ADD_TODO:

        case DELETE_TODO:

        case CLEAR_COMPLETED:
    }

}